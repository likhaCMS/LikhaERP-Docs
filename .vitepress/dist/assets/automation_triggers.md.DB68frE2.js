import{_ as t,o,c as n,ai as a}from"./chunks/framework.2e-g_11C.js";const p=JSON.parse('{"title":"Triggers","description":"","frontmatter":{},"headers":[],"relativePath":"automation/triggers.md","filePath":"automation/triggers.md"}'),i={name:"automation/triggers.md"};function s(r,e,l,h,c,d){return o(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="triggers" tabindex="-1">Triggers <a class="header-anchor" href="#triggers" aria-label="Permalink to “Triggers”">​</a></h1><p>Triggers define the action or events that start flows.</p><p>A trigger defines the event that starts a flow. This could be from an internal or external activity, such as changes to data, logins, errors, incoming webhooks, schedules, operations from other flows, or even the click of a button within the management interface.</p><p>There are 5 types of triggers: event hook, webhook, schedule (CRON), &quot;another flow&quot;, and manual.</p><h2 id="event-hook" tabindex="-1">Event Hook <a class="header-anchor" href="#event-hook" aria-label="Permalink to “Event Hook”">​</a></h2><p>Event Hooks are triggered by events within the platform. The logic is based on API extensions. Any data generated by the event will be nested in the <code>$trigger</code>.</p><ul><li><strong>Type</strong> — Choose the type of event hook: <ul><li>Filter (Blocking) — This pauses the transaction and passes the <code>payload</code> to your flow, allowing you to validate or transform <code>payload</code> or even prevent the event transaction.</li><li>Action (Non-Blocking) — Actions do not block the event. A non-blocking action is mostly useful for completing tasks in response to an event, without modifying the event or slowing the API.</li></ul></li><li><strong>Scope</strong> — Set the types of events that trip this trigger.</li><li><strong>Collections</strong> — Set the collections whose events trip this trigger.</li><li><strong>Response Body</strong> — This is optional and only for &quot;Filter (Blocking)&quot; events. It defines data to replace the event&#39;s original <code>payload</code>. Choose to return: <ul><li><strong>Data of Last Operation</strong> — Replaces event <code>payload</code> with value from <code>$last</code>.</li><li><strong>All Data</strong> — Replaces event <code>payload</code> with the entire data chain.</li><li><strong>Other</strong> — Replaces event <code>payload</code> with value from an <code>&lt;operationKey&gt;</code>.</li></ul></li></ul><blockquote><p><strong>Events with Multiple Items</strong> If you create items in a single request, the flow will trigger once for each item. If you create three items, the flow runs three separate times. On each run, just one item will be in the payload.</p></blockquote><h3 id="filters" tabindex="-1">Filters <a class="header-anchor" href="#filters" aria-label="Permalink to “Filters”">​</a></h3><p>A filter will halt the event transaction, copy the event <code>payload</code> into the flow, let the whole flow run, optionally replace the original <code>payload</code> by configuring the response body, then resume the event&#39;s transaction to the database.</p><blockquote><p>If you&#39;re filtering on the <code>items.created</code> scope, the item itself will not have an ID, as it hasn&#39;t been created yet.</p></blockquote><blockquote><p><strong>Response Body</strong> If no <strong>Response Body</strong> is configured, the original payload will not be modified, but you&#39;d still have the ability to cancel the transaction.</p></blockquote><blockquote><p><strong>Cancelling Transactions</strong> To completely cancel a transaction, you&#39;ll need to throw an error within a script operation or end the Flow on a failure path.</p></blockquote><h3 id="actions" tabindex="-1">Actions <a class="header-anchor" href="#actions" aria-label="Permalink to “Actions”">​</a></h3><p>An action lets the event commit its transaction to the database without waiting for the Flow to finish. These are asynchronous, leading them to be more performant, but without the ability to validate or transform the payload before the transaction is committed to the database. However, a copy of the <code>payload</code> is still added into <code>$trigger</code> to use as needed.</p><h2 id="webhook" tabindex="-1">Webhook <a class="header-anchor" href="#webhook" aria-label="Permalink to “Webhook”">​</a></h2><p>Webhooks are triggered on an incoming HTTP request to: <code>/flows/trigger/:this-webhook-trigger-id</code> which you can copy from the webhook trigger panel.</p><ul><li><strong>Method</strong> — Choose whether the Flow will be triggered by a <code>GET</code> or <code>POST</code> request from the dropdown.</li><li><strong>Asynchronous</strong> — Toggle whether or not the trigger responds asynchronously.</li><li><strong>Response Body</strong> — Optional. Defines data to return as a response. Choose to return: <ul><li><strong>Data of Last Operation</strong> — Responds with value from <code>$last</code>.</li><li><strong>All Data</strong> — Responds with the entire data chain.</li><li><strong>Other</strong> — Responds with value from an <code>&lt;operationKey&gt;</code>.</li></ul></li><li><strong>Error on Reject</strong> - Choose whether an error should be thrown if the final operation rejects.</li><li><strong>Cache</strong> — Choose whether responses to <code>GET</code> requests should be stored and served from cache or the cache should be bypassed.</li></ul><blockquote><p><strong>Response Body</strong> If no data is defined in <strong>Response Body</strong>, the response is empty.</p></blockquote><blockquote><p><strong>Asynchronous</strong> If <strong>Asynchronous</strong> is enabled, the Flows system will immediately resolve/respond to the request without returning a <strong>Response Body</strong>, which frees up the API immediately. If it is disabled, the Flows system will wait for the Flow to finish and return whatever value is in <strong>Response Body</strong>. This slows the API, but lets you send data.</p></blockquote><h2 id="schedule-cron" tabindex="-1">Schedule (CRON) <a class="header-anchor" href="#schedule-cron" aria-label="Permalink to “Schedule (CRON)”">​</a></h2><p>This trigger type enables creating data at scheduled intervals, via <a href="https://github.com/kelektiv/node-cron#readme" target="_blank" rel="noreferrer">6-point cron job syntax</a>.</p><ul><li><strong>Interval</strong> — Set the cron job interval to schedule when the Flow triggers.</li></ul><h3 id="syntax-explanation" tabindex="-1">Syntax Explanation <a class="header-anchor" href="#syntax-explanation" aria-label="Permalink to “Syntax Explanation”">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span> ┌────────────── second (0-59)</span></span>
<span class="line"><span> │ ┌──────────── minute (0-59)</span></span>
<span class="line"><span> │ │ ┌────────── hour (0-23)</span></span>
<span class="line"><span> │ │ │ ┌──────── day of month (1-31)</span></span>
<span class="line"><span> │ │ │ │ ┌────── month (1-12)</span></span>
<span class="line"><span> │ │ │ │ │ ┌──── day of week (0-7)</span></span>
<span class="line"><span> │ │ │ │ │ │</span></span>
<span class="line"><span> │ │ │ │ │ │</span></span>
<span class="line"><span> * * * * * *</span></span></code></pre></div><h2 id="another-flow" tabindex="-1">Another Flow <a class="header-anchor" href="#another-flow" aria-label="Permalink to “Another Flow”">​</a></h2><p>This trigger type executes by the trigger Flow operation, allowing chaining Flows.</p><ul><li><strong>Response Body</strong> — Optional. Defines data to return and append under the <code>&lt;operationKey&gt;</code> of the trigger Flow operation that tripped the trigger. Choose to return: <ul><li><strong>Data of Last Operation</strong> — Responds with value from <code>$last</code>.</li><li><strong>All Data</strong> — Responds with the entire data chain.</li><li><strong>Other</strong> — Responds with value from an <code>&lt;operationKey&gt;</code>.</li></ul></li></ul><blockquote><p><strong>Flows for-loops</strong> If you pass an array to this trigger, it will run once for each item in the array.</p></blockquote><h2 id="manual" tabindex="-1">Manual <a class="header-anchor" href="#manual" aria-label="Permalink to “Manual”">​</a></h2><p>This trigger starts your Flow on a manual click of a button within the management interface.</p><p>The manual trigger must take in item ID(s) before you can click it. On the collection page, the button will be grayed out until you select some number of items. From the item page, the current item&#39;s ID is passed in automatically. These item IDs are passed in to <code>$trigger</code> as an array.</p><ul><li><strong>Collections</strong> — Choose the Collection(s) to add the button to.</li><li><strong>Location</strong> — Choose to display the button on the Item Page, Collection Page, or both.</li><li><strong>Asynchronous</strong> — Toggle whether the Flow executes asynchronously. If enabled, you can immediately trigger the Flow again. If not, you must wait for the Flow to complete to use it again.</li><li><strong>Collection Page (Requires Selection)</strong> — Toggle whether a selection is required in the Collection Page to trigger.</li><li><strong>Require Confirmation</strong> - Toggle whether a confirmation dialog will be shown before the Flow is executed.</li><li><strong>Error on Reject</strong> - Choose whether an error should be thrown if the final operation rejects.</li></ul><p>After the operation runs, a toast notification will appear in your sidebar indicating whether the Flow ran successfully.</p><h3 id="confirmation-dialog" tabindex="-1">Confirmation Dialog <a class="header-anchor" href="#confirmation-dialog" aria-label="Permalink to “Confirmation Dialog”">​</a></h3><p>If enabled, a confirmation dialog will be shown in a modal before the Flow is executed. There are further options to set up a confirmation dialog.</p><ul><li><strong>Confirmation Description</strong> - Text shown at the top of the modal.</li><li><strong>Confirmation Input Fields</strong> - Set up one or more inputs to be filled by users before executing the Flow.</li></ul><p>Each input field can have its own data type, interface, and display options. Some convenience options are also provided to immediately alter the user input (such as trimming whitespace and slugifying text).</p><p>Data provided by users when triggering a manual Flow with a confirmation dialog will be accessible in <code>$trigger.body</code> in the data chain.</p>`,39)])])}const u=t(i,[["render",s]]);export{p as __pageData,u as default};
